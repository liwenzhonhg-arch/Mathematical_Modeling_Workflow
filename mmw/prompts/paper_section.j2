请根据以下建模全过程的资料，撰写完整的竞赛论文。

## 问题分析

{{ analysis }}

## 模型假设

{{ assumptions }}

## 数学模型

{{ model }}

## 求解结果

{{ results }}

{% if interpretation %}
## 结果解释

{{ interpretation }}
{% endif %}

{% if figures %}
## 生成的图表

{% for fig in figures %}
- {{ fig }}
{% endfor %}
（请在论文中适当位置引用这些图表）
{% endif %}

## 写作硬约束

- 论文中所有结果性数值只能来自“求解结果”和“结果解释”中真实出现的数值；不要自行推导新增百分比、倍数、峰值或阈值。
- 如果某个子问题缺少可引用数值，应在论文中保守表述，并提示需要补充求解结果，不能编造。
- 只能引用上方“生成的图表”列表中的图片，不得臆造图名。
- 若模型没有真实标签，不得写准确率、召回率、混淆矩阵等监督验证结论。

请按照系统提示中的论文结构和写作规范，为每个章节输出独立的 LaTeX artifact。
