请根据以下数学模型编写完整的 Python 求解代码。

## 数学模型

{{ model }}

## 模型参数

{{ params }}

{% if data_summary %}
## 数据说明

{{ data_summary }}
{% endif %}

## 验证报告中的建议

{{ verify_notes }}

{% if data_files %}
## 数据文件清单（data/raw/ 下的真实文件，文件名必须严格使用以下名称）

{% for f in data_files %}
- `data/raw/{{ f }}`
{% endfor %}

**铁律：只能读取上述真实数据文件，严禁生成模拟/示例/合成数据。**若读取失败，打印 `os.listdir('data/raw')` 后 raise 终止，不许用假数据继续。
{% endif %}

{% if deliverables %}
## 题目硬性交付文件（一个不能少）

题目明确要求提交以下结果文件，solution.py 必须在工作目录生成它们（xlsx 用 `pandas.DataFrame.to_excel`，列结构严格按题目规定）：

{% for d in deliverables %}
- `{{ d.file }}` —— {{ d.desc }}
{% endfor %}

缺任何一个文件即视为求解未完成，这是题目评分的硬性要求。
{% endif %}

代码文件保存后将在工作目录下运行，数据文件在 `data/raw/`，图表请保存到 `figures/`。

**必须产出**（详见系统提示规范 10、11）：
- `results.json` —— 论文将引用的全部关键数值，缺失会导致论文数据无出处
- 灵敏度分析模块 —— `sensitivity.json` + 每个参数的灵敏度图

请按照系统提示中的格式要求输出 solution.py 和 code_explanation.md。
