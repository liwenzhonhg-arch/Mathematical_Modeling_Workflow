请仅修复 Verifier 判定为 block 的问题，不做无关重写。

{% if problem_text %}
## 原始题面

{{ problem_text }}
{% endif %}

{% if research_evidence %}
## 调研证据状态

{{ research_evidence }}

未执行的搜索不能作为参数来源；代码尚未运行时不得声称已经得到拟合参数、RMSE 或 R²。
{% endif %}

## 当前模型

{{ model }}

## 当前方程

{{ equations }}

## 当前参数

{{ params }}

## 结构化验证结论

{{ verify_status }}

## 验证报告

{{ verify_report }}

要求：

1. 逐项修复 block issue，并在模型正文中明确修改后的公式、边界或约束。
2. 没有被 block 指出的部分保持不变。
3. 重新输出完整的 `model.md`、`equations.json`、`params.json` 三个 artifact。
4. 若失败证据包含 NaN/Inf，必须修改模型本身：为定义域外场景给出有限窗口指标或结构化不可用状态，禁止只在代码中截断或换成任意常数。
5. 若失败证据指出参数未标定或基准参数与数据矛盾，必须修改 `params.json` 中对应数值或需求/服务率公式，并在 `model.md` 写明数据依据；仅增加“超出适用范围”、上限或不可行状态不算修复。
6. 若题目要求优化方案，修订后的合理基准场景必须存在非退化可行解；不得用全零指标、所有场景相同决策或任意截断制造通过结果。
7. 输出前做一次逐符号闭环检查：每个目标函数、约束、状态转移和指标中出现的符号，都必须在 `model.md` 的符号表或紧邻公式处定义其计算式、单位、数据来源/代理口径与不可用分支；不得留下“归一化量”“基准值”“进入量”等只有名称没有计算式的符号。
8. 输出前做一次分支穷尽检查：每条队列派发、容量或资格规则必须覆盖所有正常状态（空/非空、达到/未达到配额、零需求、零车辆、样本不足）；每个分母须有正性条件，不满足时输出结构化“不可用”并排除该场景排序，禁止用 \(\epsilon\) 代替业务分母。
9. `equations.json` 必须是 `model.md` 中可直接实现的压缩版，而非提纲：目标里出现的归一化量、参考值、代理输入、分流变量和状态量必须同时给出定义或引用明确编号公式；不得只在正文中隐含。
10. 在 `model.md` 末尾加入“Verifier 修复核对表”，逐条列出本轮每个 block issue、对应修复公式/约束位置，以及该位置如何保证可计算和有限输出。不要声称已修复却遗漏该表。
11. 下游真实执行证据优先于原模型假设：若 code 日志已经出现 `Bi >= 0.1`、`R² < 0.90`、归一化 RMSE 超过 10%，或合理搜索域内持续无可行解，不得保留原结构后仅添加警告、扩大参数边界或继续调惩罚权重。必须切换到先前声明的升级结构（如空间离散、状态/位置相关参数或其他更完整机理），并同步重写方程、参数和求解步骤。
12. 修订后的验证计划必须能让 Coder 用真实附件直接执行：明确候选结构比较指标、升级触发条件、失败时停止位置，以及最终只允许通过标定阈值的结构进入优化。
13. 若 block issue 来自模型自行增加、题面未要求的约束、稳健认证或验证层，优先删除该结构并恢复题面可行域，不要继续为它增加参数、统计检验或区间算法。
14. 移动热过程修订不得手写新的 PDE 离散公式；保留连续方程并引用受测 `_mmw_moving_heat`。`surface_transfer_rates` 直接使用与厚度倒数同单位的 Robin 系数 \(\gamma=h/\lambda\)，不得另行换成依赖网格的 `1/time` 速率。若真实多起点证据证明 PDE 的扩散率与 Robin 参数不可辨识，删除不可辨识物理参数，改用受测的少量炉区组经验一阶响应模型 \(dT/dt=k_g(x)(T_{\mathrm{air}}-T)\)；响应率按题面不同设定值的受控炉区组及冷却区设置，受控温区之间的环境温度固定为平台加真实间隙线性过渡，末受控温区之后若为无设定温度的冷却通道，则按题面实际通道几何连续过渡到炉外温度，不得伪造室温平台或任意选取过渡终点；不再标定过渡形状参数。2020A 的结构辅助回归中，环境温度从小温区9终点线性下降到小温区10与11之间的间隙中点，此后保持炉外温度，有效交换率仍在全部相邻温区的真实间隙中点切换。\(k_g\) 才使用 `1/time`，且只能解释为同设备中心温度的有效响应率。题面若明确进入设备开始计时，附件非零首时刻就是物理时刻，传感器稍后开始工作只表示缺少前缀记录，不能再加阈值穿越时刻。模块按 `position=speed*time` 移动，题面为 `cm/min` 且时间为秒时须先把速度换成 `cm/s`。候选工况从固定几何入口重算，参数稳定性使用多起点诊断；不得引入题面未提供的二维几何；不得再引入全域样条、跨工况事件位置或连续置信域认证。
15. 若 code 反馈已经用真实运行证据否决 m1/m2 等 PDE 候选，修订后的现役 formulation、`equations.json` 和方法契约只能把最终降阶结构列为硬约束；被否决候选只保留在结构选择依据中，不得继续要求 Coder 在每个新版本重复实现。缺少被否决候选的运行实现不是硬约束缺失。
16. 经验降阶结构必须报告分区残差，但分区残差默认是结构偏差诊断。除非阈值来自题面或独立验证并已预先声明，不得用任意 `区域均值残差 / 全局 RMSE` 比例单独触发结构否决、`MODEL_REWORK_REQUIRED` 或重新启用已由 code 证据否决的 PDE；偏差应写入局限性，并由问题 2～4 的原题硬约束和网格复核继续把关。
17. 若 code 运行证据明确 `strict_*_certificate=false`、列出未完全实现约束或说明运行环境没有区间 ODE/Interval Newton API，且题面未要求形式化连续域证明，删除该不可执行验证层和超出实际运行预算的固定次数，改用有明确容差的粗细网格收敛复核并把最优性限定为实际检查集合；不得要求 Coder 仅补写 `covers`。
18. 多个优化子问题必须共享 Coder 单次执行的总墙钟上限：先为标定、问题2固定扫描、文件输出和门禁预留时间，再分配 q3、q4 搜索。不得把完整执行上限分别设为每个子问题的默认预算。每个候选开始前检查共同搜索截止；已启动候选最多只能越过搜索截止且必须在总截止前完成，否则中断并丢弃，不得使用部分结果。固定必答工作超时须携带实际耗时失败，不能缩减后伪装完成。
19. 经验一阶响应率场 \(k_g(x)\) 必须在从炉前区到炉后区的完整路径上逐段唯一，明确受控区、组内/组间真实间隙、冷却区及端点取值；不得只列五组参数而留下 ODE 右端未定义区间。输出前核对 `model.md`、`equations.json` 和方法契约引用的每个公式编号真实存在且一致。
20. 当前 Coder 的 `run_python_code` 单次外部超时已知为 300 秒。缺少显式输入时直接使用 `B_total_default=300 s`，并在同一轮通过 `time.monotonic()` 记录实际耗时、为尾部输出保留安全余量；不得要求外部另传预算、先执行一轮测时后停止，或依赖跨运行 runtime profile 才正式求解。
21. 2020A 问题2必须完成 701 点固定初扫，但边界精化只针对初扫实际发现的状态变化区间。不得假定全部 700 个相邻区间都变化、预留 4200 次精化并据此拒绝启动；固定工作若真实超过同一轮预算，携带实际耗时失败。
22. 数据、拟合或可辨识性门禁失败时允许结构化停止，模型不需要保证任意输入都能答完四问。即使 Verifier 建议“增加升级路线”，只要下游 code 证据已经否决 PDE、传感器状态或其他候选，现役 formulation 仍只能保留选中的经验降阶结构；不得为满足建议重新引入已淘汰模型。
23. 参数跨数量级并在 \(\log\theta\) 坐标中优化时，触边距离必须按对数搜索区间计算。不得使用线性区间宽度的1%误判对数域内部参数，也不得借此扩大既有参数上下界。可辨识性只比较相对全局最佳损失在预声明容差内的近最优结果，并与受测 `assess_multistart_identifiability` 语义一致。
24. 问题2若明确把结果限定为完整固定网格上的最大可行值，并声明 `global_certificate=false`、不声称连续速度域最大值，则删除自加的连续边界认证；不得让 Verifier 的连续域建议重新引入已删除的二分层。阈值穿越规则必须覆盖采样点恰等于阈值和连续阈值平台，使用含等号的确定性端点语义。
25. 修订模型不得改写题目分析或验收契约中的规范结果名。若结果口径收窄为固定网格或条件预测，保留原键并在描述中说明；需要额外状态时新增键，不用新键替代原键。
26. 真实运行证据或明确重做要求已经删除 Sobol、随机搜索、连续精化等可选候选生成器时，修订版不得以“强制工作完成后若有余量再追加”为由重新引入。单轮墙钟必须同时覆盖标定、问题1、固定扫描、正式候选、指标复用和文件输出；用于建立耗时估计的前几个任务也必须在启动前先检查对应绝对截止和共同总截止，不能把试运行置于门禁之外。
27. 多起点模式搜索要求每个起点至少完成初值和首轮全部方向时，先计算最低调用数 `N_start * (1 + N_direction)` 并与该轮共享调用上限及实测墙钟核对。最低调用数已超过上限时，必须减少确定性起点数并把结论限定为有限多起点 `best-found`；不得保留一个运行前即可证明无法启动的搜索合同。
28. 有效平板路径的可辨识性只使用受测 `assess_multistart_identifiability` 的原始门禁语义：传入至少3个成功起点的全部终值与损失，不按参数去重；不得叠加 SVD、条件数或逐参数剖面优化硬门禁。单工况迁移限制写成 `conditional_prediction` 和 `external_validation: unavailable`，不通过增加超预算验证层解决。
29. 若真实 code 证据表明问题2完整701点扫描存在可行速度、而问题3固定33次搜索为零可行候选，问题3三个起点改为问题2可行速度升序序列的首项、中位索引项、末项，并沿用问题2固定设温；不足3个不同速度时允许重复。仍完成 `3 * (1 + 10) = 33` 次评价，不扩大搜索、不调松门禁、不写入 Oracle 数值。
30. 若中心对称容器的正负倾角可由探针零偏或初始绝对容量平移得到同一容量曲线，改为在单一非负幅值参数域做多起点标定，并把符号写成等价集合/不可辨识披露；不得把这组物理等价解当作参数不稳定。空、满平台是分段容量函数的合法分支，不得再用“全部训练区间两端必须部分充液”筛掉候选。
31. 定位图示显示同一水平尺寸链时，逐段核对每个标签。内部探针定位把主体分成左右两段时，主体总长必须包含两侧尺寸；不得只取其中一段作为圆柱长度。
32. 原始问题的“图形定位文本”来自官方 DOCX 矢量 shape，不是 OCR。若坐标顺序已形成闭合尺寸链，直接把几何状态改为 confirmed 并执行标定/制表；不得继续设置只能由 Coder 人工看图解除的 `geometry_unconfirmed` 或以此输出空结果。
33. 同一水平尺寸序列没有原文或独立线端点证据表明嵌套时，按横坐标顺序作为连续相邻段；不得仅凭常见结构把内部定位段解释成另一段的子集。若仍存在两种有证据的解释，用有限离散候选和同一附件残差/可辨识性门禁选择，不得任意覆盖现役尺寸链。
34. 容量、库存或累计流量序列缺少绝对初值时，使用“模型状态=初始存量+累计净流量”拟合完整轨迹，把初始存量作为 nuisance parameter；题面或附件备注已给初始存量时固定使用该值，累计量不得首行归零或另行重估初值。不得只拟合相邻增量，也不得在题面未声明时增加传感器零偏/比例系数来吸收几何误差。
35. 题面要求完整标定表时，按题面给定的观测量名义范围和固定步长对齐主网格；物理空端、满端可附加但不能替代必答网格，观测样本范围外只作外推披露。拟合阈值不得由同一候选的训练 RMSE 乘常数循环放宽；没有独立阈值时使用 code 的预声明归一化拟合门禁。
36. 截面图存在两个或多个尺寸、但提取文本没有可靠绑定横轴/竖轴时，不得继续把任一方向称为“题面直接给定”；改成有限轴向绑定候选，并由同一附件的预声明残差、物理边界和留出误差选择，不读取 Oracle。
