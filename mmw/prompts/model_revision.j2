请根据 Verifier 的 block 证据，重写一份完整、单一现役的模型。

{% if problem_text %}
## 原始题面
{{ problem_text }}
{% endif %}

{% if research_evidence %}
## 调研证据状态
{{ research_evidence }}

未执行的搜索不能作为参数来源；代码尚未运行时不得声称已经得到拟合参数、误差或最优值。
{% endif %}

## 当前模型
{{ model }}

## 当前方程合同
{{ equations }}

## 当前参数合同
{{ params }}

## 结构化验证结论
{{ verify_status }}

## 验证报告
{{ verify_report }}

## 修订规则

1. 只改变与 block issue 有关的定义、公式、约束、方法或验证口径，但必须重新输出完整的 `params.json`、`equations.json` 和 `model.md`。
2. “保持有效内容”是语义要求，不是输出格式。禁止追加版本修复章节、旧模型全文、Verifier 核对表或 vN 合同；禁止写“沿用原式”“其余不变”等引用旧版的省略语。
3. `model.md` 每个顶层子问题只能出现一次，正文只表达修订后的当前定义。修订证据与版本差异由系统写入 `revision_history.json`。
4. 保持 analyze 阶段的规范子问题 ID 和必答输出名。数据清洗、拟合、搜索等内部步骤不得扩成新顶层问题。
5. 继续严格区分题面事实、硬约束、定义、实现选择和真正模型假设。若 block 来自模型自行增加且题面未要求的约束或验证层，优先删除并恢复题面可行域，不要增加更多假设修补它。
6. `equations.json` 必须使用 schema v2，并让每个子问题形成：题目要求 -> 输入 -> 逻辑链 -> 目标/约束 -> 方法 -> 输出 -> 验证。所有 `assumption_refs` 必须引用现有 `ASM-*` ID。
7. 真实下游运行证据优先于原模型偏好。已被证据否决的候选不得重新进入现役 formulation；应改用仍受证据支持的结构，或在无法闭合时结构化停止。
8. 每个符号、分母、状态转移和指标必须可计算，给出单位、数据来源、定义域与不可用分支。NaN/Inf 不能作为结果，也不能靠任意截断掩盖。
9. 方法必须写明选择理由与确定性停止条件。默认无墙钟上限；只有用户显式设置 `MMW_MAX_RUNTIME_SECONDS` 时才可启用保护墙钟，触发后标记 incomplete 并阻断审批。
10. 输出前检查三个 artifact 的 ID、公式、目标、约束、假设引用、方法和必答输出完全一致。
