请仅修复 Verifier 判定为 block 的问题，不做无关重写。

{% if problem_text %}
## 原始题面

{{ problem_text }}
{% endif %}

{% if research_evidence %}
## 调研证据状态

{{ research_evidence }}

未执行的搜索不能作为参数来源；代码尚未运行时不得声称已经得到拟合参数、RMSE 或 R²。
{% endif %}

## 当前模型

{{ model }}

## 当前方程

{{ equations }}

## 当前参数

{{ params }}

## 结构化验证结论

{{ verify_status }}

## 验证报告

{{ verify_report }}

要求：

1. 逐项修复 block issue，并在模型正文中明确修改后的公式、边界或约束。
2. 没有被 block 指出的部分保持不变。
3. 重新输出完整的 `model.md`、`equations.json`、`params.json` 三个 artifact。
4. 若失败证据包含 NaN/Inf，必须修改模型本身：为定义域外场景给出有限窗口指标或结构化不可用状态，禁止只在代码中截断或换成任意常数。
5. 若失败证据指出参数未标定或基准参数与数据矛盾，必须修改 `params.json` 中对应数值或需求/服务率公式，并在 `model.md` 写明数据依据；仅增加“超出适用范围”、上限或不可行状态不算修复。
6. 若题目要求优化方案，修订后的合理基准场景必须存在非退化可行解；不得用全零指标、所有场景相同决策或任意截断制造通过结果。
7. 输出前做一次逐符号闭环检查：每个目标函数、约束、状态转移和指标中出现的符号，都必须在 `model.md` 的符号表或紧邻公式处定义其计算式、单位、数据来源/代理口径与不可用分支；不得留下“归一化量”“基准值”“进入量”等只有名称没有计算式的符号。
8. 输出前做一次分支穷尽检查：每条队列派发、容量或资格规则必须覆盖所有正常状态（空/非空、达到/未达到配额、零需求、零车辆、样本不足）；每个分母须有正性条件，不满足时输出结构化“不可用”并排除该场景排序，禁止用 \(\epsilon\) 代替业务分母。
9. `equations.json` 必须是 `model.md` 中可直接实现的压缩版，而非提纲：目标里出现的归一化量、参考值、代理输入、分流变量和状态量必须同时给出定义或引用明确编号公式；不得只在正文中隐含。
10. 在 `model.md` 末尾加入“Verifier 修复核对表”，逐条列出本轮每个 block issue、对应修复公式/约束位置，以及该位置如何保证可计算和有限输出。不要声称已修复却遗漏该表。
11. 下游真实执行证据优先于原模型假设：若 code 日志已经出现 `Bi >= 0.1`、`R² < 0.90`、归一化 RMSE 超过 10%，或合理搜索域内持续无可行解，不得保留原结构后仅添加警告、扩大参数边界或继续调惩罚权重。必须切换到先前声明的升级结构（如空间离散、状态/位置相关参数或其他更完整机理），并同步重写方程、参数和求解步骤。
12. 修订后的验证计划必须能让 Coder 用真实附件直接执行：明确候选结构比较指标、升级触发条件、失败时停止位置，以及最终只允许通过标定阈值的结构进入优化。
13. 若 block issue 来自模型自行增加、题面未要求的约束、稳健认证或验证层，优先删除该结构并恢复题面可行域，不要继续为它增加参数、统计检验或区间算法。
14. 移动热过程修订不得手写新的 PDE 离散公式；保留连续方程并引用受测 `_mmw_moving_heat`。温区采用平台值加间隙过渡，候选工况从固定几何入口重算，参数稳定性使用多起点诊断；不得再引入全域样条、跨工况事件位置或连续置信域认证。
