请评审以下数学建模竞赛论文。

## 论文各章节

{% for name, content in sections.items() %}
### {{ name }}
```latex
{{ content }}
```

{% endfor %}

{% if numeric_audit %}
## 数值审计结果（程序化比对，高置信缺出处的数字极可能是编造的）

{{ numeric_audit }}

审计是辅助线索而非判决：低置信可疑项可能是合理表述，但高置信缺出处项必须在评审意见中逐一指出并要求核实。
{% endif %}

## 特别检查

- 若存在高置信缺出处数值，必须降低结果可信度评分，并建议回到 solve/code 阶段补结构化结果或删除该数字。
- 若论文承认真实标签不可观测，不要建议用监督学习准确率作为验证；请建议代理验证、敏感性分析或补充可观测标签来源。
- 若题面和附件确实缺少逐次事件、实时车辆、医院或外援等动态输入，且论文已把相应结果明确降格为静态代理、容量下界或不可用状态，不要仅因没有虚构动态结果而判 fail；应判 warning，并要求最终可信等级保持 scenario-feasible 或 unverified。只有论文虚称完成、遗漏边界，或现有数据足以计算却未计算时才判 fail。
- `artifact_manifest.txt` 同时列出当前检查点文件和正式导出路径；清单中出现的 `output/figures/*` 与 `code/solution.py (export path)` 视为已存在。尚未执行最终编译时，只能把 PDF 页数和浮动体版式列为 warning，不能据此判 fail。
- `human_rework_feedback.txt` 是本轮人工复核意见，必须逐项响应；它不是论文正文，不得要求引用。
- 检查 `checklist.json` 中是否覆盖参考文献、附录代码、硬性交付物和数值可追溯性。

请按照系统提示中的评审标准进行审查，输出 review.md 和 checklist.json。
