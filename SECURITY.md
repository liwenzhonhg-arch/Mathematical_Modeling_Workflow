# 安全策略

## 支持范围

安全修复以最新 GitHub Release 和默认分支为主。旧版本可能不再单独回补，请先确认问题能否在最新版本复现。

重点关注以下边界：

- 生成代码的动态执行、子进程和网络访问限制。
- API Key、token、Codex 登录态和日志脱敏。
- GUI 的本机会话、项目路径绑定与目录穿越。
- 文件上传、题目附件解析和输出文件访问。
- Windows 更新器的摘要校验、解压体积和路径穿越防护。
- benchmark 隔离，尤其是参考答案或验收范围泄漏到 Agent 输入。

## 报告安全问题

优先使用仓库 **Security** 页面中的私密漏洞报告功能。请提供：

1. 受影响版本、提交或 Release。
2. 最小复现步骤。
3. 实际结果与预期结果。
4. 影响范围和必要的日志片段。
5. 已验证的缓解方式（如有）。

不要提交真实 API Key、token、Codex 会话文件、个人数据或完整竞赛私有数据。如果仓库暂未开放私密漏洞报告，请只创建不含利用细节的 Issue，请求仓库维护者建立私密联系渠道。

## 非安全问题

模型回答错误、提示词质量、普通数值偏差和没有越权影响的程序异常，通常属于质量或可靠性问题，请使用普通 Issue 报告。若问题能够绕过质量门禁、泄漏 evaluator-only 数据、读取项目目录外文件或暴露凭据，则按安全问题处理。

## 项目安全边界

MMW 会对生成代码执行静态检查并清理子进程中的密钥变量，但不提供操作系统级沙箱。处理不可信题目附件、参考资料或生成代码时，应使用隔离账户或虚拟机。
