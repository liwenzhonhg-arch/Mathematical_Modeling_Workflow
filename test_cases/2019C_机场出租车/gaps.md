# 缺陷与待改进清单 — 2019C

分类：[工具] mmw 代码 / [提示词] prompts/ / [人工] 赛时人脑攻坚。修复后打勾并注明修复方式。

## 严重（影响得分）

- [x] **[工具] code 阶段缺少 `solution.py` 仍标记 completed**：本轮 `code` 检查点最初只有 `code_explanation.md`、`meta.json`、`status.json`，没有 `solution.py` 和 `run_log.txt`，但阶段状态仍为 completed。→ 已在 `stage_code.py` 增加 `_has_solution_py()` 门禁，缺代码时拒绝保存 completed 检查点，并新增 `tests/test_stage_code_gate.py`。
- [x] **[提示词/工具] Coder artifact 格式不稳**：Coder 可能输出普通 Markdown 代码块而非 `solution.py` artifact，导致检查点无法保存代码。→ 现有 Coder 已有裸代码块兜底；本轮新增 stage_code 门禁，保证兜底失败时不会误完成。
- [x] **[提示词] Research v1 忽略监督学习标签可观测性**：v1 主推 Logistic/决策树，但本题缺少可靠的“送客司机是否排队”真实标签。→ 已在 `system/researcher.j2` 和 `research.j2` 增加“标签可观测性铁律”。
- [x] **[提示词→人工修订] Writer 扩写出大量无出处数值**：review 阶段数值审计发现 26 个高置信缺出处数值。→ 第二轮补强 `results.json` 并手工修订 `paper v3`，纯代码审计确认 89 个数值全部匹配，缺出处清零。
- [x] **[人工] q3 推荐上车点数量为 1 的结论不可信**：该结果与优秀案例和机场运营常识不一致。→ 第二轮改用高峰设计流量 `418.4` 辆/小时和单点服务率 `40.0` 辆/小时，推荐 `14` 个上车点。

## 中等（影响质量）

- [x] **[提示词] Model/Verifier 对“真实标签不可观测”约束不够敏感**：虽然后续人工修正了 `model.md`，但模型产物仍出现过准确率、召回率、混淆矩阵等不成立表述。→ 已在 `system/modeler.j2`、`model.j2`、`system/verifier.j2`、`verify.j2` 加入数据可观测性和工程常识性审查。
- [x] **[提示词] Reviewer 又建议构建分类器验证司机决策**：review 中建议用逻辑回归比较真实司机决策准确率，但本轮数据并无可靠真实决策标签。→ 已在 `system/reviewer.j2` 和 `review.j2` 明确：真实标签不可观测时不得建议监督学习准确率作为主验证。
- [x] **[工具→代码产物补强] 数值审计不能区分 EDA 统计值与求解结果来源**：`1769`、`1198`、`67.7%` 等来自 EDA 或原始数据统计，但未进入 `results.json`，因此被判高置信缺出处。→ 第二轮把 EDA 统计写入 `results.json`，审计通过。
- [x] **[工具→代码产物补强] `results.json` 指标粒度不足**：摘要评审指出问题二缺少系统吞吐量具体数值。→ 第二轮新增 q1/q2/q3/q4 关键指标，`paper v3` 和 `review v2` 均基于新版结果。
- [x] **[提示词→人工修订] Paper 阶段摘要达标但质量偏低**：摘要评分 85 分刚好达标，问题包括吞吐量缺失、方法名不够具体、字数偏短。→ 第二轮手工压缩摘要并补足方法与结果，review v2 给摘要质量 9/10。
- [ ] **[人工] 当前题面不是官方原文**：`problem.md` 基于公开题意整理，后续应替换为官方 C 题 PDF 的准确题面。
- [ ] **[人工] 当前数据不是官方原始附件**：出租车数据来自 GitHub 处理后 CSV，机场到达行程只有 71 条，限制模型验证。

## 轻微（体验/健壮性）

- [x] **[工具] solve 清理临时脚本导致成功阶段返回失败**：Windows 下 `solution.py` 删除触发 `PermissionError`，原逻辑在保存 solve 检查点后仍抛异常。→ 已新增 `_cleanup_temp_script()`，清理失败只提示，测试覆盖 `PermissionError`。
- [x] **[工具] solve 清理失败时提示级别过重**：临时脚本 `PermissionError` 已改用 `print_warning`，保持非阻断语义并有回归测试。
- [x] **[工具] pytest 沙箱内临时目录权限异常**：当前运行环境下已直接完成全量 pytest，不再复现 `PermissionError`；历史不可访问目录仍等待清理授权。
- [x] **[工具→代码产物修订] Matplotlib 中文字体缺失**：手工运行 `solution.py` 时出现 CJK glyph 缺失警告，图中的中文标签可能显示异常。→ 第二轮将图表标签改为英文，手工运行无字体警告。
- [x] **[工具] pytest 失败留下不可访问临时目录**：当前工作树已不存在 `test_cases/pytest_tmp*`，只保留 `.gitignore` 防止同类临时目录进入版本库。

## 已修复（本次实测中）

- [x] **[提示词→人工约束] 方法主线从监督分类改为期望收益/排队论/仿真/公平性优化**：通过 `references/method_constraints.md` 引导 research v2 修正。
- [x] **[人工修复] `model.md` 中不成立的监督学习验证表述已改为代理验证**：避免在无标签数据上承诺准确率、召回率或混淆矩阵。
- [x] **[工具] `test_cases/pytest_tmp*/` 已加入 `.gitignore`**：避免不可访问临时目录影响后续 git 上传。

## 第四轮新增发现（2026-07-19）

- [x] **[工具] Verifier 解析失败时复用旧通过状态**：模型修订轮次曾把历史 `verify_status.json=pass` 合并进新版本。→ `stage_model.py` 现在会在每轮验证前剥离旧验证报告、状态和历史，并新增回归测试。
- [x] **[工具] Solve 允许遗漏整个子问题的结果**：早期版本没有任何 `q3_*` 指标仍能通过。→ 状态机现按 `sub_problems.json` 检查每个子问题至少有一项结构化结果。
- [x] **[工具] Solve 未拦截基尼系数和吞吐量下降越界**：曾出现基尼系数 `1.1211`、吞吐量下降 `-3.8304`。→ 增加 `[0,1]` 物理范围门禁。
- [x] **[提示词] Coder 产生 NaN/Inf、字符串状态和无信息灵敏度**：通过有限数值、状态编码、过载分支和非零灵敏度要求强化提示词；最终 v22/v17 通过。
- [x] **[工具] Reviewer 结构化清单把“否”恢复成 pass**：review v8 明确写出“附录代码是否完整（否）”及多个实质问题，但 fallback `checklist.json` 全部为 pass，`rework_stage` 为 none。→ Reviewer 现以明确否定和“严重/关键问题”覆盖勾选状态，并根据独立失败项推断最早责任阶段；review v22 为 0 fail。
- [x] **[提示词/人工] 摘要缺失率口径错误**：最终摘要把 `1.3544` 写成“缺失率”，该值大于 1，Reviewer 已指出但未触发返工。→ Solve 对缺失率、概率、比例增加 `[0,1]`/`[0,100]` 门禁，最终缺失率为 `0.6772`。
- [x] **[模型/人工] q3 结果过度理想**：推荐 19 个上车点且等待时间四舍五入为 `0.00` 分钟，参数 `mu0=60 辆/小时` 和需求率 `80 辆/小时` 的标定依据不足。→ 经 Reviewer 回退 model 并重建代码，最终推荐 `2` 个上车点，等待 `0.0041` 小时，繁忙率 `0.4444`。
- [ ] **[人工] q2 仍无法做真实决策验证**：最终结果正确标记 `validation_available=0`，说明现有处理后数据不足以验证“送客司机排队或空返”的真实行为。

## 第五轮新增与关闭（2026-07-19）

- [x] **[工具] 图表比例门禁扫错目录**：Solve 检查点不保存 PNG，原门禁无法发现 2065×19939 的巨大图例图。→ 改为按当前版本 `figures_list.json` 精确检查 `workspace/figures`，缺图、非法路径和超过 4:1 的比例均拒绝审批。
- [x] **[工具] Paper 可在有图时不嵌入正文**：Writer 只写“图已保存”仍可审批。→ Paper 门禁要求全部核心 `fig_*.png` 出现在 `\includegraphics`，缺失时定向修订 `model_solution.tex`。
- [x] **[工具] 灵敏度百分比可与 objective/baseline 矛盾**：曾出现 objective 全为 0，但 change_pct 达数千百分比。→ 增加公式一致性和零基准规则，并要求至少两个非零敏感参数。
- [x] **[工具] 负数哨兵可冒充物理结果**：曾以 `-1` 表示无上车点、无等待时间和无步行距离。→ 对数量、时间、距离、长度等非负量增加物理范围门禁。
- [x] **[工具] Windows 检查点保存偶发 `PermissionError`**：临时目录替换正式版本时会被短暂占用。→ `CheckpointManager.save` 对替换操作进行有限重试并有回归测试。
- [x] **[工具] 高置信缺出处数值总是回退上游代码**：简单的论文派生值会触发无谓的整链重跑。→ 纯论文数值审计失败优先回退 paper；只有明确点名 `results.json`、`solution.py` 或求解失败时才回退 code。
- [x] **[提示词/工具] 验证数据不可用时容易陷入“逼迫伪造成功”循环**：真实标签和代理序列均不可用时，Coder/Reviewer 反复要求验证状态为 1。→ 允许明确写明原因的“验证不可用”作为 warning，同时继续拒绝未说明原因的验证/校准失败。
- [ ] **[人工] 问题2验证仍是最终成品的主要短板**：现有 GPS 处理数据形成常数代理序列，无法区分真实司机决策。需要官方原始附件、可靠状态标签或独立观测数据才能实质解决。
