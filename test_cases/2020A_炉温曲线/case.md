# 案例：2020 国赛 A 题“炉温曲线”盲测

## 测试性质

- 轮次：三题盲测第 2 题
- 测试日期：2026-07-10
- 快照 SHA-256：`825815dd405e8b112a6d39a94e3de5e60983d14595d8222802946dde2c4f7a22`
- 题面、测温附件和提交模板来自 `yushugulao/CUMCM-Archive` 的公开赛题目录。
- `references/` 为空，未向 Agent 提供优秀论文、评阅要点、题解或参考代码。
- 未人工修改任何检查点产物、代码、数据、图表或论文。

## 运行配置干预

Coder 专用 API Key 与第 1 题相同，无法通过认证。为继续检验工作流后半程，仅在当次进程中让 Coder 使用已工作的默认 Key，没有修改 `.env`、提示词或任何产物。干预记录保存在 workspace 的 `logs/blind_interventions.jsonl`。

## 阶段结果

| 阶段 | 版本 | 结果 |
|---|---:|---|
| analyze | v1 | 正确拆为 4 个子问题，并识别题目要求提交 `result.csv` |
| eda | v1 | 生成 5 张图并提取峰值、升降温速率等特征；无法正确读取 GBK 提交模板，且把 150–190°C 持续时间算为 253.50 s，结果可疑 |
| research | v1 | 给出集总热平衡、参数辨识和多目标优化路线；外部检索标记未真正获得文献证据 |
| model | v1 | 建立参数辨识与优化框架；Verifier 指出时间原点和参数辨识问题，却又错误建议把题面固定为 25°C 的 10–11 温区作为决策变量 |
| code | v1 | 两次运行均因 `AttributeError: module 'numpy' has no attribute 'trapz'` 失败；阶段仍保存为 completed |
| solve | v1 | 同样执行失败；`results.json=[]`、`sensitivity.json={}`，却仍保存 completed，并收集到部分旧图或半成品图 |
| paper | v1 | 在无求解结果时编造 `RMSE=1.52°C`、最大速度 `93.5 cm/min` 等结论；摘要评分 48 且 `needs_upstream_data=true`，阶段仍保存 completed |
| review | v1 | 审计发现 38 个高置信缺出处数值；Reviewer 发现摘要/正文互相矛盾、约束不满足、问题三四无结果，但阶段仍可审批 |

## 流水线报告与真实状态的差异

- CLI 最终允许 8 个阶段全部审批，并成功编译 PDF、生成 `submission.zip`。
- 实际代码和求解失败，题目要求的 `result.csv` 没有被有效生成，关键结果与灵敏度为空。
- 论文摘要声称最大速度 93.5 cm/min，正文又写 65 cm/min；正文给出的峰值 224.31°C 和高温持续时间 31 s 直接违反题面约束。
- `review` 已明确标记这些问题为 fail，仍没有阻止审批、编译和导出。

## Token

- 输入：93,394
- 输出：51,073
- 合计：144,467

## 自主版暂评

| 维度 | 得分 |
|---|---:|
| 流程完成度 | 7/15 |
| 硬交付物 | 2/15 |
| 结果可复现 | 0/15 |
| 硬约束满足 | 0/20 |
| 模型正确性 | 5/20 |
| 论文与证据一致 | 1/15 |
| **总分** | **15/100** |

这不是对论文奖项的模拟评定，而是对工作流“能否独立形成可信交付物”的暂评。

## 结论

第二题重复了第 1 题的失败链，并增加了一个更危险的信号：Reviewer 已经精确识别出摘要矛盾和硬约束违规，系统仍把它当作可交付成品。评审目前只是建议文本，不是质量门禁。

## 第 2 轮：质量门禁回归（2026-07-10）

- 新 workspace：`blind2_2020_cumcm_A`，未复用首轮检查点。
- EDA 使用 `gb18030` 成功读取 GBK 的 `result.csv`；同时打印 0.5 s 中位采样间隔，并按连续区间积分得到 150–190°C 总持续时间 126.5 s，不再错误报告 253.5 s。
- analyze 新增幻觉 3 个中文 Excel 文件，程序只保留题面逐字出现的 `result.csv`。
- Verifier 返回 `warning`，model 可审批；随后 code 调用遇到无效专用 Key，认证错误立即返回退出码 1，不再重复请求三次，也没有 code 检查点可审批。
- 流水线未进入 solve/paper/review，未生成伪提交包。

本轮同时验证了编码探测、连续区间算法提示、交付物证据过滤和认证错误快速失败。

## 第 3 轮：运行前配置与兼容性收口（2026-07-10）

- `mmw check-config` 在流水线开始前识别 Coder 专用配置为 HTTP 401，其余两组配置可用；输出只显示 Key 末 4 位。
- 当前环境版本摘要会传给 Coder；NumPy 不提供 `np.trapz` 时，代码在执行前改用 `np.trapezoid`，并有自动化测试覆盖。
- 因 `.env` 属于用户配置且未经确认，本轮没有修改 Key，也没有伪装完成 code/solve 回归。
- Verifier 提示已明确禁止把题面固定变量改成决策变量。model v2～v4 经自动修订仍为 block；再次以 v4 的结构化问题定向修订后生成 v5，Verifier 降为 warning，v5 已审批激活。

当前唯一阻塞是 Coder Key；修复后应从 code 阶段继续，不需要重跑已经通过并激活到 v5 的 analyze～model。

## 第 4 轮：Coder Key 修复后实跑（2026-07-10）

- Coder 配置验证通过，code 阶段正常调用。
- 之前的 `np.trapz` 错误没有再出现，兼容修复有效。
- 新错误为 `NameError: ok_q4 is not defined`；反思后第二轮仍是同一错误。
- code v1 无法审批，后续阶段未运行。

结论：环境兼容问题已解决，但 Coder 对跨子问题结果变量的完整性检查不足。

## 第 5 轮：Coder 反思增强后回归（2026-07-10）

- 未定义的 `ok_q4` 错误未再次出现。
- 新生成程序连续 3 次超过 300 秒执行上限；两轮升级反思都没有降低计算规模。
- code v2 审批被拒绝，solve/paper 未运行。

结论：变量完整性提示有效，但 Coder 缺少计算预算意识和超时专用降复杂度策略。

## 第 6 轮：超时预算与占位结果门禁（2026-07-11）

- code v3 第一次运行超时后进入专用复杂度反思，第二版在 300 秒内完成，不再无条件消耗第三个超时周期。
- 运行日志同时暴露新的高风险行为：问题 2 无可行解时“使用默认值输出占位结果”，问题 3 把 `1e10` 罚函数值当作最优面积，问题 4 没有可行 Pareto 解。
- 状态机新增明确占位结果门禁；`approve code` 返回 1，因此没有进入 solve/paper。

结论：计算预算提示能让程序完成，但“运行完成”不等于“求解可信”；占位输出已被提升为硬失败。

## 第 7 轮：跨检查点修订与模型回退（2026-07-11）

- code 重跑不再从零生成，而是读取上一版 `solution.py`、运行日志和门禁错误进行定向修订。
- v4/v5 均可在一次超时反思后运行完成，但仍报告问题2无满足约束速度、问题3目标为罚函数值、问题4无可行 Pareto 解；扩展门禁均拒绝继续。
- 把 code v5 的真实失败证据交回 model v5 定向修订，生成 model v6/v7。
- Verifier 对 v6/v7 均判定 `block`；v7 仍缺传感器随体坐标、炉前/炉后温度定义，并使用不可信的绝热边界和等效长度。

结论：2020A 的根因不是再调 Coder，而是模型本身无法解释实测曲线。流水线已从重复 code 空转改为回退 model 并在结构错误处停止。

## 第 8 轮：完整论文链路反审与模型再回退（2026-07-20）

- 使用 DeepSeek V4 Pro 从 model v12 继续，code v8、solve v2、paper v5 一度完成并通过阶段门禁。
- 最终 review v1 识别出两个根本错误：问题 2 擅自修改题面固定温区得到 `55 cm/min`；问题 1 拟合仅 `R²=0.265`、预测 `>217°C` 时长为 0。系统没有继续导出，而是按 `rework_stage=model` 回退。
- model v14～v17 依次修正题面温度、温度调整范围、参数量纲和空间单位，Verifier 最终对 v17 判定 `pass`。
- 实际代码验证仍无法得到可行解。code v15 只能给出违反约束的惩罚近似解：速度 `99.29 cm/min`、峰值 `252.86°C`、最大升温斜率 `7.2393°C/s`、150～190°C 持续 `315.98s`。
- code v15 已被质量门禁阻断，未审批；旧 solve v2、paper v5、review v1 均显示上游已变更，不能作为当前模型的有效产物。
- 本轮修复了五类工具漏洞：摘要高分但超 600 字提前停止、优化失败后使用参考解、跨 model 版本复用旧代码、流式 `httpx` 超时未重试、截断代码空运行和违反约束近似解被误判成功。

结论：工作流现在能在最终评审发现根本错误后自动回退并阻止伪论文传播，但 V4 Pro 生成的 2020A 热模型仍未通过真实可行性验证。当前可信状态是“模型自动验证通过、代码实证失败”，不是“完整论文已产出”。

## 第 9 轮：确定性公开基线与参考契约（2026-07-20）

- 参考来源采用 `CUMCM/2020-A` 的完整 MATLAB 仿真代码，并用独立仓库 `personqianduixue/CUMCM2020A` 的论文结果交叉确定宽容范围。
- 新增 `reference_solver.py`，逐式移植温度场、位置相关换热系数和板内显式差分；把 100 个子步合并为仿射矩阵幂，完整基线约 2.4 秒运行完毕。
- 基线复现：问题一温区 3/6/7 中点和温区 8 结束温度分别为 `130.62/168.54/189.50/223.46°C`；问题二固定题面温度下最大允许速度约 `77.06 cm/min`。
- 新增 `reference_expected.json`，验收使用交叉范围而非单篇题解精确值。工作区存在该文件时，Coder 提示、code 门禁和 solve 门禁都会要求指定结果名与范围。
- code v16 收到契约后输出了标准字段，但仍把违反斜率和持续时间约束的近似解当结果；原有可行性门禁先行阻断。独立参考校验还发现温区 8 结束温度 `239.52°C` 超出 `[218,235]`。
- 全量回归：`247 passed`。

结论：2020A 现在有独立于 LLM 的可执行基准。后续模型或代码只有同时通过题面硬约束和公开基线范围，才可进入 solve；Verifier 的文字 `pass` 不再是此题的唯一正确性依据。

## 第 10 轮：隐藏 Oracle 与无人值守完整链路（2026-07-21）

### 工具链修改

- `reference_expected.json` 改为 evaluator-only Oracle，不再复制到工作区、传给 Coder 或写入普通检查点；旧检查点中的契约保留但被正常状态机忽略。
- 新增 `mmw benchmark --case <案例> --workspace <工作区> --stage code|solve`，独立检查通用门禁、上游陈旧状态和公开基线范围，并生成 JSON/Markdown 报告。
- code 阶段无论是否有 Oracle，都只保存本轮新写入的 `results.json` 预览；旧文件没有变化时拒绝作为新结果。
- Coder 现在读取题目原文以保留题面明确要求的点值和交付项，但不会读取 Oracle；结果名称允许由 Oracle 显式 aliases 做非数值匹配。
- 新增“约束满足: False”“无可行解后使用替代”门禁；失败运行的 stdout 会进入检查点和下一轮反思，不再只保留 traceback。
- Modeler/Verifier 增加模型简化可证性要求：不能只因材料薄就断言 Bi<0.1；低质量时间序列拟合必须升级模型结构后再优化。

### r1：完整运行到 paper 后被阻断

- 全新 workspace：`benchmark_2020A_20260721_r1`；只复制 `problem.md` 和 `data/raw/`，`references/` 为空。
- analyze、eda、research、model、code、solve 均生成并审批；paper v1 因 `needs_upstream_data=true` 无法审批，review 未运行。
- 代码日志实际写有问题2/3“约束满足: False”，问题4全部候选不可行后使用问题3解并把折衷面积/对称性写成 0；新增门禁回放后 code/solve 均判定失败。
- 隐藏 benchmark：通用门禁 FAIL；问题2结果为 `60.0 cm/min`，Oracle FAIL；问题一指定点值未进入 results.json。
- 纯本地数值审计返回退出码 1：抽取 112 个论文数值，发现 1 个高置信缺出处数值。
- Token：输入 64,886，输出 48,612，合计 113,498。

### r2：模型修订后在 code 诚实失败

- 全新 workspace：`benchmark_2020A_20260721_r2`；仍未复制任何答案或旧产物。
- Verifier 首轮 block，自动修订后的 model v2 获 pass；analyze～model 已审批。
- code 连续 5 版均明确 raise，最终标定仅 `R²=0.7626`、`RMSE=25.6289°C`，最接近候选峰值 `230.74°C`，无法满足问题2制程约束；没有 results 预览，也没有进入 solve/paper。
- Token：输入 272,727，输出 104,171，合计 376,898。

### r3：外部余额阻塞

- 全新 workspace：`benchmark_2020A_20260721_r3`；输入隔离检查通过。
- 第一个 analyze 请求即由 DeepSeek 返回 HTTP 402 `Insufficient Balance`，没有生成检查点，也没有消耗可记录 Token。
- 按无人值守计划停止继续请求；未修改 `.env`，未切换私有 Codex 后端，也未用人工答案改写生成代码。

### 验证与结论

- 定向测试 80 项通过；全量测试 `262 passed`；`compileall` 与 `git diff --check` 通过。
- `paper.pdf`、compile 和 export 没有刷新：r1 的 paper 未通过质量门，r2 未到 paper，r3 被外部余额阻塞。`deliverables/` 中旧 PDF 仍是历史快照，不代表本轮成功。
- 本轮确认“假成功传播”已被阻断，但没有完成一份同时通过通用门禁、隐藏 Oracle、数值审计和论文编译的全新论文。当前状态必须报告为 **blocked**，不能报告成功。

## 第 11 轮：无人值守 r4 与执行后质量闭环（2026-07-23）

### 运行范围

- 全新 workspace：`benchmark_2020A_20260723_r4`；仅使用题面、附件和原始 `result.csv`，隐藏 Oracle 未进入任何 Agent 提示或普通检查点。
- analyze、eda、research 已审批；model 经下游证据和 Verifier 循环迭代至 v13 并审批。
- code 最终生成 v7，但运行失败且未审批；solve、paper、review、compile、export 均未继续。

### 实跑发现

- model v10 对应的 code v5 把“未找到严格可行速度，选择最接近方案”当作成功；隐藏 benchmark 同时发现结果字段不完整且关键答案缺失。
- 修复后，code v6 虽正常退出，但 `results.json` 明确写入 `q2_可行性=0`；新门禁将这种“进程成功、结论失败”识别为失败。
- 下游失败证据回传 model 后：
  - v11 为制造可行性引入未标定的速度指数 `n=0.5`，且保留 23 个独立参数，未审批。
  - v12 将参数缩减为 `k_zone/k_gap/k_front/k_rear`，但 Verifier 判定速度幂律导致全局无解并给出 block。
  - v13 移除未经标定的速度幂律并通过 Verifier；真实代码标定却只有 `R²=0.7798/0.7799/0.7405`，随后两版优化超过 300 秒，最终 v7 失败。

### 工具修复

- Coder 候选在每次生成和修订后写入 `recovery.json`；网络中断后可从同一 model 版本恢复，不重复生成。
- Coder 不再只看 Python 退出码：运行成功后立即检查失败语句、`results.json` 新鲜度、JSON schema 和显式验证/可行性状态；失败会进入同一反思循环。
- code 审批新增 `results_preview.json` schema 与 `可行性=0` 检查；识别“未找到可行解后采用最接近方案”。
- 反思提示包含门禁的 `error_summary`，避免只看到 stdout 而漏掉语义失败原因。
- model 阶段获得完整题面；下游 code 失败证据优先于未审批 warning 进入模型修订。
- 失败代码检查点不再显示“代码实现完成”的成功文案。

### 结论

r4 验证了新的自动恢复和质量闭环，但仍未产出真实可实施解。当前确定性停止点是：**4 参数简化模型无法达到通用拟合门槛，继续自动改代码只会在低拟合、超时和伪可行之间循环**。因此不应进入论文阶段，也不能把 v5/v6 或历史 PDF 当作完成结果。

## 第 12 轮：结构辅助回归 r5（2026-07-24）

### 隔离与结构增强

- 全新 workspace：`benchmark_2020A_20260724_structured_r5`；只复制题面与附件，未复制旧检查点、旧结果、参考求解器或隐藏 Oracle。
- 本轮不是盲测，明确标记为“结构辅助回归”：只从确定性参考实现提取移动坐标、内部一维瞬态导热、位置相关环境/换热剖面、数值稳定性与低维标定等通用结构；未把参考答案、验收范围或专用参数传入 Agent。
- 新增通用 `moving_heat` 仿真工具、移动热过程知识条目和 3 项合成数据回归；合成换热尺度可由独立最小二乘恢复，失稳网格会被拒绝。
- model 增加确定性证据门禁：禁止在代码执行前声称已取得拟合参数/误差，禁止把未执行检索写成证据，Research 指定一维导热为主结构时不得把 PDE 仅作为备用路径。

### 实跑结果

- analyze v1、eda v1、research v1 已审批。
- model 经过 v1～v14 的证据门禁和下游反馈修订，v14 最终采用移动坐标、一维瞬态导热、Robin 边界及分区换热参数，Verifier=`warning` 后审批。
- code 共形成 v1～v3 三个失败检查点，均未审批：
  - v1：二参数集总模型虽然达到 `R²=0.9385`，但问题 2 无可行速度；
  - v2：三参数 PDE 最终标定 `R²=-0.2374`、`NRMSE=0.8992`；
  - v3：分区 PDE 在标定不足、问题 2 无可行速度和 300 秒超时之间循环，五次反思后仍失败。
- 因 code 通用门禁未通过，未运行 solve、paper、review、compile、export，也未执行隐藏 benchmark；没有可被误报为成品的新论文。

### 工程验证与结论

- 全量测试 `285 passed`；`compileall` 与 `git diff --check` 通过。
- r5 证明通用物理结构、证据隔离和合成回归均已落地，但单条实测中心温度曲线不足以稳定辨识内部扩散与多个位置换热参数，Agent 仍会在欠参数化、不可辨识和计算超时之间摆动。
- 当前可信状态仍为 **blocked at code**。下一步应新增“参数可辨识性/多起点一致性”确定性门禁，并让 Coder 复用仓库内已验证的通用仿真器，而不是继续让 LLM 每轮重写 PDE 求解器。

## 第 13 轮：多起点可辨识性门禁（2026-07-29）

- 在既有 `_mmw_moving_heat` 中增加通用多起点诊断，不包含 2020A 答案、Oracle
  范围或专用常数。
- 诊断要求至少 3 个初值，比较近最优参数相对跨度及可选下游结果跨度；阈值只能
  收紧，不能调宽绕过失败。
- Coder 必须实际调用诊断并在 `results.json` 写入 `参数可辨识性=1`；候选执行和
  `PipelineStateMachine` 审批会分别复核，缺失或值为 0 均阻断。
- 合成数据已覆盖一致收敛、等损失多参数、下游结果分叉及非法输入；历史 r5
  “任选一组参数继续”的路径现在会停在 code。
- 针对性回归 `117 passed`，全量回归 `410 passed`；本轮未调用 LLM。

本轮只证明工具能识别并阻断不可辨识标定，尚未重新消耗真实 LLM 运行 2020A，
因此不能把该题标记为已求解。

## 第 14 轮：真实 Codex 托管回归 r6/r7（2026-07-29）

### r6：发现并修复反馈失真

- 隔离 workspace：`benchmark_2020A_20260729_identifiability_r6`；只复制题面和原始
  `附件.xlsx`，使用本机 Codex 登录态，未把隐藏 Oracle 传入 Agent。
- analyze、eda、research 通过；model 到 v9 仍为 `block`，未进入 code/solve。
- 首次暂停暴露出托管重做只把“Verifier 发现严重问题”交回 Modeler，覆盖
  `verify_status.json` 和完整报告。修复后恢复运行，v6/v7 的历史已保留 v5 的 5 条
  具体 issue，后续问题推进到 Robin 离散、跨工况时间基准和题面约束完整性，证明
  反馈保真修复生效。
- r6 同时证明旧 Codex 适配器不记录 token，`800000` 配置无法实际熔断；因此停止把
  r6 当作预算验证，新增 `codex exec --json` usage 解析。

### r7：带真实 token 证据的预算回归

- 隔离 workspace：`benchmark_2020A_20260729_budgeted_r7`；输入隔离方式同 r6。
- Codex usage 已进入检查点：analyze `27894`、eda `61941`、research `36302` tokens；
  model v1～v5 分别为 `86882`、`170704`、`276551`、`88748`、`171701`。
- 托管最初在 model v5 后因 `800000` token 预算暂停并显示 `920723`；复核 14 份
  唯一调用日志后，发现真实不重复累计为 `695020`。旧算法把 model 各版本中的累计
  Agent usage 重复相加，造成提前暂停和错误汇报。
- 修复后改为优先汇总唯一调用日志，并在每次供应商请求返回 usage 后立即熔断。
  r7 从 `695020` 恢复：第一项请求后为 `794666`；下一项请求完成后为 `885632`，
  随即以“请求边界预算已用尽”暂停，未再调用下一轮 Verifier。
- 新实现把越界限制为当前单次请求，本次相对 `800000` 超出 `85632`；供应商在请求
  完成前不返回最终 usage，因此它仍不是绝对零超出的硬上限。
- model v5 最终仍为 `block`：验证窗与分区覆盖冲突、额外单调性改变题面可行域、
  连续近优集合认证不可执行、Hermite 温场结构缺少平台型对照。未进入 code、solve、
  paper、review，也未运行隐藏 Oracle。

结论：真实 Codex token 计量、唯一请求去重、请求边界熔断和 Verifier 反馈链均已
修复并验证；完整题目仍停在 model。设置 token 预算现在能阻止同阶段后续请求，但
不能表述为供应商单次请求也绝不会越过上限。

## 第 15 轮：最小模型与 code 真实回归 r8（2026-07-29）

### 隔离与预算

- 隔离 workspace：`benchmark_2020A_20260729_minimal_r8`；只复制题面与原始
  `附件.xlsx`，隐藏 Oracle 未进入 Agent 提示、普通检查点或 recovery。
- 首轮预算为 `800000` token、240 活跃分钟；从已审批 model v4 的第二次 code
  诊断使用独立续跑预算 `350000` token、120 活跃分钟。
- 首轮到 `810948` token 在 code 暂停；续跑从该基线新增 `407707` token，在
  28.2 活跃分钟后按请求边界暂停。

### 流程进展

- analyze、eda、research 均为 v1 并审批。
- model v1～v3 被 Verifier 阻断；应用“只保留题面硬约束、连续 PDE/Robin 和受测
  模块接口”的最小模型纪律后，model v4 通过 Verifier 并审批。相比 r7 停在 model，
  r8 在累计 `557363` token 时进入 code，证明最小化修复有效。
- code 的旧 recovery 先因把隐式格式套用显式扩散数门槛而失败。修正提示后，下一
  候选不再触发该门槛，并把可辨识性函数的原始报告直接写入顶层。
- 后续真实失败依次暴露：局部补丁没有主流程、三参数标定不可辨识、修订候选删除
  受测模块、基准拟合 `R2=-7.423147`/`NRMSE=0.719181`。所有候选均被 code
  门禁阻断，未审批，也未进入 solve、paper、review 或隐藏 benchmark。

### 由真实失败驱动的工具修复

- `diffusion_number <= 0.5` 只约束显式格式；隐式格式改用网格/时间步收敛验证。
- `identifiability.json` 必须直接保存诊断函数原始返回对象，其他元数据另存。
- 受测移动热模块的 `surface_transfer_rates` 统一为 Robin 系数
  `gamma=h/lambda`，边界数按 `2*r*gamma*dz` 计算，不再与 `1/time` 混用。
- 明确 `position=speed*time` 的单位契约：题面为 `cm/min`、采样时间为秒时传入
  `speed/60`。
- Coder 不再用缺少既有硬输出或受测模块调用的局部补丁覆盖完整 recovery。
- token 熔断改为保留当前已完成响应和候选，在下一次请求前停止；每次 usage 同时
  落盘并更新进度。

### 确定性对照

- 使用同一 r8 失败候选和修正后的 Robin 模块，未修速度单位时 NRMSE 从原退化值
  `0.719181` 改善到 `0.270448`。
- 再只把模块入参从 `70 cm/min` 改为 `70/60 cm/s`，NRMSE 进一步降到
  `0.096197`；这证明两项单位错配均为独立根因，而不是为了通过门禁调阈值。
- 该候选仍命中扩散率搜索下界、没有报告 R²，且尚未完成问题 2～4，所以此对照不
  是完整解，也不能进入隐藏 Oracle。
- 针对性回归 `88 passed`，全量回归 `418 passed`。

### 修复后第二次 code 续跑

- 新增 `500000` token、120 活跃分钟预算；实际使用 `451428` token、24.1 分钟。
  每次 Codex 请求完成后，`managed-run.json` 和进度回调立即显示
  `47667 -> 117367 -> ... -> 451428`，验证实时用量修复有效。
- 首个完整候选使用正确 Robin/速度单位，真实得到 `R²=0.849298`，被拟合门禁拒绝。
- 后续候选自行加入观测时间偏移，得到 `R²=0.950572`、`NRMSE=0.055092`，但结果
  `delta_t_obs=-20.872426 s` 直接违反 model v4 的硬约束 `delta_t_obs=0`。
- code v2 只实现问题1。方法契约门禁发现问题2～4硬约束未覆盖后，旧逻辑只修改
  `implementation.covers`，相同代码被保存为 v3；solve 随后两次确定性报告
  `results.json 缺少子问题结果: q2, q3, q4`，并发现 `result.csv` 缺失。
- 流程按重做预算在 solve 暂停，没有进入 paper/review/benchmark。该轮证明方法契约
  的自报覆盖不能替代结构化结果。

### 第二次续跑后的修复

- 删除“缺硬约束时只修契约”的分支，必须同时返回完整 `solution.py` 和契约。
- code 执行后立即按 analyze 的子问题 ID 检查 `q1_`～`q4_` 结果，不再等 solve。
- 首次定向修订若是局部补丁，先作为失败进入反思，不再静默执行旧代码并保存成功版。
- 新增模型契约与结果交叉复核：硬约束固定时间偏移为 0 时，非零偏移结果直接失败。
  r8 的 `-20.872426 s` 已被确定性回放拒绝。
- 全量回归更新为 `421 passed`。

结论：r8 首次让真实 Codex 流程越过 model 并系统暴露 code 的物理/单位契约错误。
当前仍是 **blocked at code**；下一轮应让修正后的 Coder 重新生成完整候选并同时
满足拟合、可辨识性、边界命中、四个子问题和硬交付物门禁。

## 第 16 轮：code → model 自动升级真实回归（2026-07-30）

### 工具修复验证

- 新托管运行首先发现 recovery 比 code v5 更新且属于同一 model v4，以
  `0 tokens` 直接恢复执行，没有重新生成代码。
- 恢复候选再次证明契约内四种过渡结构无法同时通过拟合质量与参数可辨识性门禁，
  系统停止 Coder 反思，保存 code v6 及归一化 `rework_request.json`。
- 托管器首次自动从 code 回退 model；Modeler 读取 code v6 的最终日志和全部候选
  摘要，model v5/v6 被 Verifier 阻断，model v7 达到 warning 并获机器激活。
- v7 首次 Coder 请求达到 900 秒超时，未返回响应、未计 token、未保存伪检查点；
  独立重试在 4 分钟、63948 tokens 后正常返回，说明这是单次供应商超时。
- v7 候选先报告基础及升级结构均未过拟合门槛；局部补丁被拒绝后旧候选没有再次
  执行。第三个完整候选使用 `MODEL_REWORK_REQUIRED`，保存 code v7 并再次回退
  model。
- 第二轮托管累计使用 `699022` tokens。model v8～v11 均被 Verifier 阻断；
  最新 v11 的决定性问题是二维升级模型在板进入/离开炉体时没有定义炉外
  `T_a`/`gamma`，行进方向端部绝热缺少依据，问题4网格索引越界，以及无全局证书
  时仍把搜索所得值形式化为全局最小值。

### 当前结论

- code → model 升级、较新 recovery 优先、模型不足短路和局部补丁不重复执行均已
  通过真实流程验证；针对性回归 `56 passed`，全量回归 `427 passed`。
- 当前停在 **model v11 / Verifier block**。没有进入新 solve、paper、review 或
  隐藏 benchmark，仍不能称为完整解。
- 下一轮应只修复 v11 的四项具体闭合性问题，不再重新设计整个模型；二维后备结构
  通过 Verifier 后再让 Coder 用真实附件检验它是否改善拟合。

## 第 17 轮：不可辨识 PDE 的有限降阶路径（2026-07-30）

- r8 继续从 model v11 修订到 v15，新增运行使用 `465517` tokens、26.4 分钟，
  但 Verifier 持续 `block`。最新版本仍加入题面没有的严格单调约束和依赖未知板长
  的二维后备模型，并存在面积排序容差小于数值误差等问题。继续增加模型复杂度已
  不能由现有观测支持。
- 新增通用 `simulate_piecewise_first_order`：当 PDE 的扩散率与 Robin 参数
  不可辨识时，只标定少量炉区组中心温度有效响应率；不把它解释为材料参数，也不
  引入题面缺失的二维几何。
- 使用 r8 原始附件、物理零时刻、固定 `70/60 cm/s`、题面温区平台与间隙过渡进行
  确定性对照。5 个区域组响应率从 3 个不同初值收敛到同一解，
  `R²=0.988284`、`NRMSE=0.026822`，既有多起点诊断判定
  `identifiable=true`。
- 上述指标只证明该有限经验结构能在同设备基准轨迹上稳定标定，不是隐藏 Oracle
  通过、材料参数辨识或跨工况外部验证。下一步必须让 Modeler 明确采用该结构，再由
  Coder 独立生成问题1～4、硬交付物和运行级最优性证据。
- 针对性回归 `60 passed`，全量回归 `431 passed`。

## 第 18 轮：降阶结构首次真实 LLM 闭环（2026-07-30）

- 新托管运行使用 `828171` tokens、49.1 分钟。model v17 获
  `Verifier=warning`，首次把经验一阶结构写入正式模型；code v8 的该结构从 5 个
  起点得到 `R²=0.962168`、`NRMSE=0.048198`，多起点和分区残差均通过。
- code v8 仍被正确阻断：模型把传感器达到30°C的穿越时刻再次加到附件时间，
  但题面已经明确电路板入炉开始计时，附件首行 `t=19 s, T=30.03°C` 是物理时刻，
  不是事件后重新归零。模型还同时标定两个环境过渡形状参数，二者分别命中上下边界，
  因此经验结构不能激活。
- 根因修订为：附件非零首时刻直接作为物理时刻；传感器启动只解释缺失前缀。经验
  降阶只标定题面不同设定值受控炉区组及冷却区的响应率，环境温度固定使用设定平台
  与真实间隙线性过渡，不再标定过渡形状。
- 针对性回归 `61 passed`，全量回归 `432 passed`。

## 第 19 轮：四个子问题首次真实执行成功（2026-07-30）

- 新托管运行使用 `834338` tokens、45.5 分钟。model v19 修正物理时钟并获
  `Verifier=warning`。code v10 首次真实执行完成四个子问题和硬输出：
  `R²=0.981294`、`NRMSE=0.033891`，32 个起点通过可辨识性诊断；
  问题2～4均产出有限条件预测、网格复核和约束状态。
- code v10 没有被错误激活。model v19 仍把已经由前序真实证据否决的 m1/m2 PDE
  候选列为现役硬约束，而 v10 只实现最终选定的 mE，方法契约因此缺少
  `CON-Q1-2/3/4`。下一次 Coder 正确用 `MODEL_REWORK_REQUIRED` 交回 model。
- 同轮还暴露代码门禁误判：`外部验证可用=0` 表示只有单工况、缺少独立实验，
  不是附件内拟合失败，却被通用“验证可用=0”规则阻断。门禁现已只豁免明确的
  `外部验证可用`，内部拟合、约束和可辨识性失败仍会阻断。
- 修订规则要求：结构选择证据已否决 PDE 后，现役 formulation 只保留最终降阶
  结构；被否决候选留作历史依据，不再要求每个代码版本重复实现。
- 针对性回归 `79 passed`，全量回归 `433 passed`。
