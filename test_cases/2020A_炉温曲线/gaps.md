# 缺陷与待改进清单 — 2020A 盲测

## 严重

- [x] **[工具] 代码兼容性错误未在生成前发现**：注入真实依赖版本，当前 NumPy 无 `np.trapz` 时执行前替换为 `np.trapezoid`。
- [x] **[工具] code/solve 失败仍可审批传播**：失败或空结果现在不能审批。
- [x] **[工具] 摘要确认数字无出处仍可审批**：`needs_upstream_data=true` 触发 paper 门禁。
- [x] **[工具] review 明确发现硬约束违规和自相矛盾仍可审批**：checklist fail 会拒绝审批和导出。
- [x] **[工具] 题目要求的 `result.csv` 未生成或校验，export 仍成功**：solve/export 均检查题面确认的硬交付物。

## 中等

- [x] **[工具] EDA 的温区持续时间算法错误或定义不清**：第二轮按两个连续区间积分为 126.5 s，并打印 0.5 s 采样间隔。
- [x] **[提示词] Verifier 对固定变量提出错误修改建议**：验证提示现在要求回看题面，固定变量不得建议改成决策变量。
- [x] **[工具] result.csv 编码识别不稳**：第二轮通过 `gb18030` 成功读取。
- [x] **[工具] solve 图表收集缺少本次运行边界**：只接受本次新增或重写产物。
- [x] **[工具] 外部检索占位语句未落地**：新增 `research_evidence.json`，明确外部检索未执行和待搜索项。

## 轻微

- [x] **[体验] Rich 流式重绘导致日志过大**：非 TTY/重定向时不启用 Live。
- [x] **[体验] 部分 Windows 子进程输出存在编码显示问题**：子进程和日志统一 UTF-8。

## 第 4 轮新增

- [x] **[工具] Coder 生成未定义结果变量**：反思提示要求检查所有读取位置和各执行路径赋值，第 5 轮未复现。
- [x] **[工具] 反思循环未验证修订是否改变根因代码**：相同错误增加升级反思机会并限制到第 3 次停止。

## 第 5 轮新增

- [x] **[工具] 超时反思没有降复杂度**：超时专用反思和连续 2 次停止已实现；v3 第二版在限时内完成。
- [x] **[提示词] Coder 缺少计算预算**：提示已要求估算 PDE/优化调用、限制 `maxiter/popsize`、缓存和粗到细搜索。

## 第 6 轮新增

- [x] **[工具] 无可行解时用默认值伪装完成**：运行日志出现“输出占位结果”时 code/solve 均不可审批。
- [ ] **[模型/代码] 热传导模型校准和可行域仍不可信**：v3 的 `R²=0.2544`，问题 2～4 没有可信可行解；需回到 model/code 修订，而不是继续论文阶段。

## 第 7 轮新增

- [x] **[工具] code 重跑丢失上一版失败证据**：复用上一版代码、运行日志和门禁错误定向修订。
- [x] **[工具] 只识别“占位结果”而漏过罚函数/无可行解**：新增罚函数值和无可行解门禁，v4/v5 均被阻断。
- [x] **[工具] code 反复失败不能回传 model**：显式重跑 model 时读取最新 code 门禁证据并进入现有 Modeler/Verifier 修订闭环。
- [x] **[模型] v7 仍缺关键物理定义**：切换 V4 Pro 后从 v7 定向修订到 v12，控制方程、Robin 边界、入口/出口环境温度、等效长度和量纲均通过 Verifier；v12 仍有参数可辨识性 warning，暂未自动审批。

## 第 8 轮新增

- [x] **[工具] 摘要达到 85 分但超过 600 字仍提前停止**：停止条件同时检查评分和正文长度；超长摘要定向压缩到 520～550 字并重新评分。
- [x] **[工具] 优化失败后借用其他子问题解仍可审批**：识别“使用替代/参考解”“无法完成子问题”“无可行或近似解”和“违反约束近似最优”等明确失败输出。
- [x] **[工具] model 激活版本变化后仍反思旧模型代码**：仅复用由当前激活 model 生成的 code 检查点。
- [x] **[工具] DeepSeek 流式响应读取超时穿透重试层**：`httpx.TimeoutException` 和 `httpx.NetworkError` 纳入公共流式重试。
- [x] **[工具] 代码被截断且没有主流程，空 stdout/stderr 仍判成功**：空运行日志现在触发 code 门禁。
- [ ] **[模型/代码] v17 Verifier pass 但真实仿真仍无可行解**：code v15 的最小惩罚解同时违反峰值、升温斜率和温区持续时间约束；需要人工建立基准实现或引入已验证的 2020A 方程/参考结果做回归测试，不能继续依赖 LLM 自证模型正确。

## 第 9 轮新增

- [x] **[工具] 2020A 缺少独立于 LLM 的确定性回归标准**：新增公开代码移植基线、交叉范围契约，以及 code/solve 可选参考门禁。
- [x] **[工具] 本次代码未写 results.json 时可能读取旧结果通过参考校验**：记录执行前文件签名，只接受本次新建或重写的结果预览。
- [x] **[工具] 损坏的参考契约会被静默忽略**：存在但超限、不可解析或 schema 非法时直接阻断 code。
- [ ] **[模型/代码] 当前生成模型仍未达到公开基线**：code v16 的温区 8 结束温度和问题二速度越界，且问题二近似解违反升温斜率与 150～190°C 持续时间约束；下一轮应基于 `reference_solver.py` 的位置相关换热结构重建模型，而不是继续调惩罚权重。

## 第 10 轮新增

- [x] **[工具] 参考答案进入 Coder 提示词造成评测污染**：Oracle 已移出正常流水线，只能由独立 benchmark 在生成结束后读取。
- [x] **[工具] 隐藏评测要求精确字段名但 Agent 不知道命名**：Oracle 支持不含数值信息的显式 aliases，仍不向 Agent暴露验收范围。
- [x] **[工具] “约束满足: False”和“无可行解后使用替代”未阻断 code**：新增运行日志门禁，r1 回放后 code/solve 均失败。
- [x] **[工具] code 失败日志丢失 stdout 诊断**：失败检查点和反思现在同时保留 stdout/stderr，r2 的 R²、RMSE 和最接近候选可直接定位。
- [x] **[提示词] Coder 看不到题目原文导致遗漏指定点值**：传入不可信标记下的题面原文，只提取任务要求，不传 Oracle。
- [x] **[提示词] 无参数证据仍断言 Bi<0.1 并采用集总模型**：Modeler/Verifier 增加简化可证性和低拟合质量升级要求。
- [ ] **[模型/代码] 仍未得到通过公开基线的全新可行解**：r1 产生违反约束的 60 cm/min，r2 在低质量标定后诚实失败；需要继续改进热传导结构，但不得把 Oracle 数值写回生成提示。
- [x] **[外部] DeepSeek 余额不足阻断最终 r3**：r4 已在新的干净工作区完成有效调用，外部余额不再是当前阻塞。

## 第 11 轮新增

- [x] **[工具] 网络中断会丢失尚未形成检查点的代码候选**：候选按 active model 版本写入 `recovery.json`，r4 已验证中断后直接恢复执行。
- [x] **[工具] 进程退出码为 0 但日志承认采用近似违规方案仍可完成 code**：Coder 执行后门禁识别“未找到可行解后采用最接近方案”，并把错误送入反思。
- [x] **[工具] code 阶段不检查结果 schema 和显式 `可行性=0`**：现在校验本轮新写入的 `results.json`、每项 `name/value/unit/desc` 及验证/约束/可行性状态。
- [x] **[工具] Coder 反思看不到执行后质量门禁的错误摘要**：反思 evidence 已加入 `error_summary`。
- [x] **[工具] Modeler/Verifier 缺少完整题面导致温区、几何和约束定义漂移**：model 与 revision 均接收题面全文，Verifier 上下文上限扩大。
- [x] **[体验] 失败代码检查点仍显示 `[OK] 代码实现完成`**：失败候选改为中性保存提示，不再伪装成功。
- [ ] **[模型/代码] 4 参数物理分组模型无法通过拟合门槛**：v13 的 code v7 多次标定仅得 `R²=0.7405~0.7799`，后续复杂化又连续超时；下一步需由独立确定性求解器抽取可推广的模型结构与合成回归测试，不能向 Agent 暴露 Oracle 答案范围。

## 第 12 轮新增

- [x] **[工具] 缺少与真题答案隔离的移动热过程结构回归**：新增通用移动平板仿真器、知识条目和合成参数恢复/稳定性测试；不含 2020A 答案、范围或专用常数。
- [x] **[工具] Modeler 可把未执行检索、未运行标定或 Research 主结构降级为已验证结论**：新增 research evidence 传递和确定性证据门禁，r5 的 model v1～v13 均按真实问题阻断或修订。
- [ ] **[模型] 单条中心温度曲线下扩散率与分区换热参数不可辨识**：r5 的不同 PDE 参数化都能出现完全不同的拟合/可行域结论；需增加多起点参数一致性、剖面似然或参数置信区间门禁。
- [x] **[代码] Coder 重复手写 PDE 求解器导致超时和离散错误**：一维瞬态导热任务必须导入沙箱临时注入的受测 `_mmw_moving_heat`，未复用会被结构门禁阻断。
- [x] **[工具] code 检查点只保留最后一次失败日志**：`attempt_history.json` 已保存每次候选的成功、超时、错误摘要和 stdout/stderr 尾部。

## 第 13 轮新增

- [x] **[工具] 多起点参数一致性只存在于提示词，没有确定性实现和审批门禁**：`_mmw_moving_heat` 新增通用多起点可辨识性诊断；一维瞬态导热代码必须实际调用，并在 `results.json` 写入通过状态，否则候选执行和 code 审批均被阻断。
- [ ] **[模型/代码] 尚未得到通过新可辨识性门禁和隐藏 Oracle 的全新 2020A 解**：本轮只完成合成回归和历史失败防传播，仍需在隔离工作区重跑 model/code；不得把工具门禁完成写成题目求解完成。

## 第 14 轮新增

- [x] **[工具] 托管 model 重做丢失 Verifier 具体反馈**：最新 model 为 `block` 时现在优先读取该版本 `verify_status.json` 和 `verify_report.md`，不再被通用重做文案覆盖；r6 恢复运行的 revision history 已验证。
- [x] **[工具] Codex 后端 token 用量恒为 0**：改用 `codex exec --json` 并只解析 `turn.completed.usage`，usage 已写入 r7 检查点和托管累计。
- [x] **[工具] token 上限会被单个长阶段越过且检查点累计重复计费**：现在优先汇总唯一调用日志，并用线程隔离的请求观察器在每次供应商 usage 返回后熔断；r7 恢复后在 `885632` 暂停并阻止下一轮 Verifier，越界已限制为当前单次请求。供应商请求完成前 usage 不可知，因此仍不得称绝对零超出的硬上限。
- [x] **[模型] 真实 Codex 回归仍无法通过 Verifier**：最小模型纪律已让 r8 model v4 通过 Verifier 并审批，流程首次进入 code。

## 第 15 轮新增

- [x] **[提示词/工具] 隐式格式被错误套用显式扩散数门槛**：显式才检查 `diffusion_number <= 0.5`；r8 续跑已越过原错误。
- [x] **[工具] `identifiability.json` 被外层元数据包装后无法复核**：诊断函数原始返回对象必须直接写入顶层，其他标定元数据另存。
- [x] **[工具/模型] Robin 系数与受测模块边界速率单位不一致**：统一 `surface_transfer_rates=gamma=h/lambda`，模块内部按 `2*r*gamma*dz` 离散，并增加解析数值回归。
- [x] **[提示词] `70 cm/min` 被直接当作 `70 cm/s` 传入移动模块**：Modeler、Coder、错误反思和运行摘要均明确 `speed*time` 单位契约；同一失败候选对照 NRMSE 从 `0.270448` 降到 `0.096197`。
- [x] **[工具] Coder 局部补丁覆盖完整 recovery**：删除既有 `results.json`、`sensitivity.json`、`method_runtime.json` 标记或受测模块调用的修订不再替换当前完整候选。
- [x] **[工具/体验] 超预算请求的已完成响应被丢弃且阶段内 token 进度不落盘**：当前响应允许解析、保存和执行，下一请求前熔断；每次 usage 更新托管状态和进度回调。
- [x] **[工具] 方法契约自报覆盖让只实现问题1的代码通过 code**：删除契约-only 修复；code 输出门禁现在按分析子问题 ID 检查结构化结果，缺 `q2/q3/q4` 会直接进入 Coder 反思。
- [x] **[工具] 定向修订被拒绝后仍执行旧代码并保存成功版本**：首次修订不完整时先产生明确失败，不再把旧候选当成本轮修订。
- [x] **[工具/模型] Coder 用新增时间偏移绕过固定零对齐硬约束**：结果与模型方法契约交叉复核；r8 的 `q1_观测时间偏移=-20.872426 s` 已被回放拒绝。
- [ ] **[模型/代码] r8 尚未得到完整可审批候选**：正确单位的无偏移候选只有 `R²=0.849298`；加入偏移虽达到 `R²=0.950572`，却违反 model v4。问题2～4和 `result.csv` 仍缺失。下一轮必须在不改变 formulation 的前提下完成四个子问题；若无偏移模型仍不能达标，应把证据交回 model，不能继续在 code 增加自由度。

## 第 16 轮新增

- [x] **[工具] Coder 已证明 formulation 不足后仍在 code 空转**：使用 `MODEL_REWORK_REQUIRED` 保存归一化重做请求，托管器已在 r8 真实运行中两次自动回退 model。
- [x] **[工具] 请求边界保存的 recovery 新于失败检查点却不被恢复**：同一 active model 下按文件更新时间优先 recovery；r8 以 0 token 重放并保存 code v6。
- [x] **[工具] 反思局部补丁被拒绝后下一轮仍重复执行旧代码**：拒绝本身成为一次失败证据，随后只接受完整候选；v7 真实日志未再重复执行旧候选。
- [ ] **[模型] 二维升级模型仍未闭合**：model v11 未定义板进出炉时炉外 `T_a`/`gamma`，端部绝热缺少尺度依据，问题4网格索引需要统一，且无全局证书时只能报告搜索所得最佳值。
- [ ] **[流程] 尚未形成完整最终作品**：最新可信状态仍为 model v11 block；solve、paper、review 和隐藏 Oracle benchmark 均未更新。

## 第 17 轮新增

- [x] **[模型/工具] PDE 参数不可辨识后只能升级到缺少板长的二维模型**：新增少量炉区组经验一阶响应 helper、SPEC、提示词与合成回归；真实附件零偏移对照达到 `R²=0.988284`、`NRMSE=0.026822`，3 个初值通过可辨识性诊断。
- [ ] **[模型/代码] 降阶结构尚未经过全新 LLM 阶段闭环**：必须从 model v15 的失败证据继续修订，由新模型和新代码独立通过四个子问题、硬交付物、solve、paper、review 与隐藏 benchmark，不能把确定性结构对照当作作品完成。

## 第 18 轮新增

- [x] **[模型/提示词] 传感器启动事件被重复当作时间零点**：题面已说明入炉开始计时，附件首个非零时刻是物理时刻；Modeler、Verifier、Coder 和知识条目均新增禁止二次叠加阈值穿越时间的门禁。
- [x] **[模型/提示词] 经验响应率已可辨识但环境过渡参数命中边界**：经验降阶固定使用设定平台与真实间隙线性过渡，只按题面不同设定值的受控炉区组及冷却区标定响应率。
- [ ] **[流程] v17/code v8 尚未形成完整作品**：经验结构虽达到 `R²=0.962168`、`NRMSE=0.048198`，但因时间语义和边界门禁未激活；修订后仍需重跑 model 至隐藏 benchmark。
