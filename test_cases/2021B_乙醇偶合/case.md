# 案例：2021 国赛 B 题“乙醇偶合制备 C4 烯烃”盲测

## 测试性质

- 轮次：三题盲测第 1 题
- 测试日期：2026-07-10
- 快照 SHA-256：`825815dd405e8b112a6d39a94e3de5e60983d14595d8222802946dde2c4f7a22`
- 题面和两个附件来自 `yushugulao/CUMCM-Archive`的公开赛题目录。
- `references/` 为空，未向 Agent 提供优秀论文、评阅要点、题解或参考代码。
- 未人工修改任何检查点产物、代码、数据、图表或论文。

## 运行配置干预

Coder 专用 API Key 在第一次 `code` 运行时返回 HTTP 401，严格自主轮因此在阶段 5 中止。为继续检验工作流后半程，新建“配置辅助轮”：仅在当次进程中让 Coder 使用已工作的默认 Key，没有修改 `.env`、提示词或任何产物。干预记录保存在 workspace 的 `logs/blind_interventions.jsonl`。

## 阶段结果

| 阶段 | 版本 | 结果 |
|---|---:|---|
| analyze | v1 | 正确拆为 4 个子问题，但把题目未要求的 `result1.xlsx`~`result3.xlsx` 幻觉为硬交付物 |
| eda | v1 | 代码成功执行并生成 11 张图；未正确前向填充附件1的合并单元格，导致“每个催化剂仅出现1次”及各催化剂平均收率结论错误 |
| research | v1 | 给出曲线拟合、多元回归、GA 优化和序贯实验设计；未实际读取 HMML 方法正文或外部文献 |
| model | v1 | 建立了完整框架；Verifier 明确指出百分比越界、离散变量连续优化、小样本 Sigmoid 过拟合和 D-最优设计基础不稳定等问题，未修改直接审批 |
| code | v1 | 内建反思后仍失败：`IndexError: single positional indexer is out-of-bounds`；阶段仍被保存为 completed |
| solve | v1 | 同样执行失败；`results.json=[]`、`sensitivity.json={}`，却仍保存 completed，并把 11 张 EDA 旧图当作 solve 图表收集 |
| paper | v1 | Writer 在无求解结果时编造 `R²>0.95`、`48.5%`、`35.2%`等结论；摘要评分 22 且 `needs_upstream_data=true`，但阶段仍保存 completed |
| review | v1 | 程序审计发现 40 个高置信缺出处数值；Reviewer 明确说结果不可信，但输出格式漂移使 `checklist.json` 未独立保存，阶段仍可审批 |

## 流水线报告与真实状态的差异

- CLI 最终显示 8/8 阶段可审批、PDF 编译成功、`submission.zip` 生成。
- 实际代码和求解失败，关键结果为空，论文包含大量编造数字，压缩包缺少被系统幻觉出的 3 个 Excel 文件。
- `export` 在明确报错“交付文件缺失”后，仍返回成功并生成 zip。

## Token

- 输入：122,959
- 输出：59,308
- 合计：182,267

## 自主版暂评

| 维度 | 得分 |
|---|---:|
| 流程完成度 | 7/15 |
| 硬交付物 | 4/15 |
| 结果可复现 | 0/15 |
| 硬约束满足 | 0/20 |
| 模型正确性 | 5/20 |
| 论文与证据一致 | 2/15 |
| **总分** | **18/100** |

这不是对论文奖项的模拟评定，而是对工作流“能否独立形成可信交付物”的暂评。

## 结论

第一题已经证实核心风险不是“Agent 不会写内容”，而是“上游失败后流水线仍持续产生看似完整的成品”。当前最优先的修复项应是失败状态和硬门禁，而不是增加新 Agent。

## 第 2 轮：质量门禁回归（2026-07-10）

- 新 workspace：`blind2_2021_cumcm_B`，未复用首轮检查点。
- analyze 仍幻觉 `result1.xlsx`~`result3.xlsx`，但程序核对题面后将三者全部过滤，不再作为硬交付物。
- EDA 已对催化剂组合编号和组合列执行 `ffill()`，修复首轮合并单元格导致的分组错误。
- Verifier 结构化判定 `severity=block`：指出收率公式未除以 100，会导致量纲和结果错误。
- `approve model` 返回退出码 1，流水线在错误模型处停止；未进入 code/solve/paper/review，未生成伪提交包。

本轮证明系统已能把 Verifier 的严重结论转化为状态机约束。

## 第 3 轮：自动修正能力回归（2026-07-10）

- 在同一训练 workspace 重新运行 model，生成 v2。
- 新模型已经修正收率百分数口径，Verifier 未再判定 block；v2 审批成功。
- 本次 v2 首次验证即通过，因此没有消耗额外修订轮次。
- 重定向日志只有 6 行、356 bytes，Rich Live 重复面板问题已消失。

当前 2021B 已从“发现量纲错误后停止”推进到“新模型通过结构化验证”；后续 code 阶段仍受 Coder Key 外部配置影响。

## 第 4 轮：Coder Key 修复后实跑（2026-07-10）

- Coder 使用默认有效 Key，成功进入代码生成与反思循环。
- 4 次执行依次暴露列名错误、催化剂编号解析错误、奇异矩阵、重复奇异矩阵。
- 最终错误：`numpy.linalg.LinAlgError: Singular matrix`，来源是直接计算 `inv(X^T X)`。
- code v1 保存失败证据，但审批门禁返回 1；solve/paper/review 均未运行。

结论：配置问题已经解决，新的真实瓶颈是 Coder 数值实现鲁棒性；门禁继续有效。

## 第 5 轮：Coder 修复后回归（2026-07-10）

- code v2 经 3 次反思后第 4 次运行成功并获批；首轮奇异矩阵问题未再阻塞。
- solve v1 生成 35 条结构化结果并获批；paper v1 生成并通过上游数据门禁。
- 摘要 Critic 发现全局模型拟合差，以及 `1000000%` 收率、`-1291.52%` 转化率等明显失真的外推结果。
- review 数值审计发现 2 个高置信缺出处数值，checklist 含 fail，审批返回 1；未编译、未导出。

结论：2021B 已从 code 推进到 review，证明 Coder 修复有效；当前瓶颈转为优化边界和 Reviewer checklist 精度。

## 第 6 轮：物理范围与评审降噪回归（2026-07-11）

- solve v2 复跑成功，但审批门禁检测到负的转化率/选择性阈值、`1000000%` 收率和 `-1751.16%` 转化率并返回 1。
- 即使旧的 solve v1 仍是激活版本，`export` 也会重新执行当前质量门禁并返回 1，不能导出历史上已审批但现已判定失真的结果。
- 符号表中的参数范围以及含 `\le`/`\ge` 的约束上下界不再作为“缺出处求解结果”报警。
- Markdown checklist 恢复会把文字明确注明“——是”的未勾选项判为 pass；没有充分证据的未勾选项改为 warning，而非一律 fail。

结论：错误优化结果现在在 solve 审批处停止，不再依赖摘要 Critic 或最终 Reviewer 才发现。
