# 缺陷跟踪

## [工具]

- [x] 人工重做理由未进入同阶段下一次修订提示。已增加按阶段和版本读取最新重做理由的反馈链。
- [x] Reviewer 把正式导出路径误判为缺失文件。已把代码、结果、灵敏度和当前图表的导出路径加入评审清单。
- [x] Writer 在 `$$...$$` 内生成 `\tag{}` 导致 xelatex 报错。组装时仅把带 `\tag` 的展示公式转换为 `equation` 环境。
- [ ] 输出目录保留旧版本图表，GUI 文件列表会同时展示历史残留；当前导出器只打包激活 solve 版本的 5 张图，因此不影响提交包。清理属于破坏性操作，未执行。

## [提示词]

- [x] Coder 修订反馈曾硬编码无关的“问题四/两车道”。已改为只依赖题面、已审批上游产物和真实文件。
- [x] Reviewer 曾把真实缺失的动态输入误判为必须生成的结果。已要求在论文正确降格为静态代理或容量下界时只列 warning。
- [ ] 论文生成仍可能输出不规范 LaTeX；当前只对已复现的 `\tag` 组合做确定性修正，其他错误继续由编译门禁暴露。

## [人工]

- [ ] 正式提交前把测试队号 `TEST-RUN` 替换为真实参赛队号并重新编译、导出。
- [ ] 若需把可信等级提升到现实可验证，必须补充 Q2 逐次呼叫/排队/车辆轨迹数据，以及 Q3 实时车辆、医院容量和外援可用性数据。
