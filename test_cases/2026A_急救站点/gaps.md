# 缺陷跟踪

## [工具]

- [x] 人工重做理由未进入同阶段下一次修订提示。已增加按阶段和版本读取最新重做理由的反馈链。
- [x] Reviewer 把正式导出路径误判为缺失文件。已把代码、结果、灵敏度和当前图表的导出路径加入评审清单。
- [x] Writer 在 `$$...$$` 内生成 `\tag{}` 导致 xelatex 报错。组装时仅把带 `\tag` 的展示公式转换为 `equation` 环境。
- [x] 输出目录可保留旧版本图表，但 GUI 成果列表现在只展示当前 solve `figures_list.json` 声明的图，不再把历史残留混成现役成果。
- [x] 编译成功后缺少独立视觉门禁。已增加 PDF/日志/图表检查、版本与 SHA256 绑定，并在失败时阻塞导出。
- [ ] 旧 solve v8 没有 `figure_manifest.json` 与逐图 CSV，不能安全重制旧 PNG；需重做 code/solve 生成结构化图表源。

## [提示词]

- [x] Coder 修订反馈曾硬编码无关的“问题四/两车道”。已改为只依赖题面、已审批上游产物和真实文件。
- [x] Reviewer 曾把真实缺失的动态输入误判为必须生成的结果。已要求在论文正确降格为静态代理或容量下界时只列 warning。
- [x] 论文生成仍可能输出不规范 LaTeX。已增加 Typesetter 两轮受约束修订、数值/引用/图表不可变契约和编译后视觉门禁；违规输出会被丢弃。

## [人工]

- [ ] 正式提交前把测试队号 `TEST-RUN` 替换为真实参赛队号；测试项目已可通过
  `allow_test_placeholders: true` 产出作品，不再阻塞功能验收。
- [ ] 若需把可信等级提升到现实可验证，必须补充 Q2 逐次呼叫/排队/车辆轨迹数据，以及 Q3 实时车辆、医院容量和外援可用性数据。
