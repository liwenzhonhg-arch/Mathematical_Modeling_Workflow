# 2010A 清洁盲测缺陷

## [工具]

- [x] 已确认 DOCX 扁平文本丢失题图标注位置，直接导致 8 m 圆柱段被误读为 6 m。
- [x] 已增加标准库 VML shape 坐标提取，并在 `problem.md` 保留同一水平线的 `1m | 2m | 6m | 1m` 顺序。
- [x] 已确认旧 benchmark 只验收标量，不能直接验收两张完整罐容表。
- [x] 已增加 evaluator-only 表格契约：文件/列、覆盖、步长、重复高度、单调性和抽样范围。
- [x] 已为 LLM token 日志增加 Agent 角色与请求耗时，旧日志保持兼容。
- [x] 托管失败优先展示结构化 Verifier/code 首要原因，不再一律折叠为 RuntimeError。
- [ ] 完成两轮 post-fix 清洁验证、PDF 视觉门禁和导出。
- [x] post-fix4_r1 暴露的附件名 NFKC 归一化、状态指标误判及“只拟合增量”根因已修复。
- [x] postfix5_r1 的 q1 横/竖椭圆轴绑定已改为有限候选，并在 postfix6_r1 正确选择半轴 `0.89 m`、`0.60 m`。
- [x] postfix6_r1 暴露的小罐相邻段误读已修复；postfix8_r1 验证总长为 `0.4 + 2.05 = 2.45 m`。
- [x] postfix8_r1 已验证小罐总长与截面半轴正确，且 q2 参数落入隐藏范围。
- [x] postfix8_r1 暴露的已知初值和主表网格问题已修复；postfix9_r1 两张主表全部冻结抽样值正确。
- [x] 隐藏 evaluator 已支持 `abs` 语义、横向倾角“幅值”别名和 `capacity_L` 列名，数值范围与抽样容差未改。
- [x] evaluator 已支持声明为 `m3/m³` 的表格列换算到 L，并补齐角度制与本轮表格文件名；未改变数值容差。
- [ ] postfix9_r1 的 `q2_物理空端容量` 非零却被放行；已增加确定性状态机门禁和 Coder 端点复算规则，待新冻结双轮验证。
- [ ] postfix10_r1 暴露互斥进/出流事件空白被扩成无证据平移参数，以及交叉验证后无唯一正式参数；规则已补齐，待新冻结双轮验证。
- [x] postfix11_r1 已验证互斥事件结构零、全数据正式重拟合和空端物理门禁生效。
- [ ] postfix11_r1 的正式表混入离网格物理端点、横偏合法结果名缺 alias；规则与 evaluator 语义已补齐，待新冻结双轮验证。
- [ ] postfix12_r1 暴露偏心探针下用候选专属初值平移证明倾角符号等价；已收紧为完整主网格绝对容量曲线判据，待新冻结双轮验证。
- [ ] postfix13_r1 暴露未收敛局部优化被当成功、稀疏候选零跨度、常量诊断 NRMSE 契约矛盾和现役 CSV 未入包；修复已实现，待新冻结双轮验证。
- [x] postfix14_r1 暴露运行预算公式含未定义尾部时间和调用数参数；postfix15_r1 的 Verifier 已确认固定 300/15/285 秒封闭合同可执行。
- [x] postfix15_r1 的探针位置、零点和方向证据冲突已在 postfix16_r1 消失，model v4 通过并进入 code。
- [ ] postfix16_r1 暴露全数据重复496点粗扫、5起点30轮与绝对 `1e-5 m³` 积分门槛共同超时；已改为复用训练成功终点重拟合和总容积相对 `1e-4` 门禁，待新冻结双轮验证。

## [提示词]

- [x] Analyst/Modeler/Verifier 已增加图示尺寸链、坐标/观测位置、空满边界和最小可执行性规则。
- [x] model 定向修订前清空旧对话，避免完整上下文逐轮叠加；累计真实 usage 保留。
- [ ] post-fix 实测确认模型不再把图中单段当作总长，也不再自增超预算统计层。
- [ ] 新冻结双轮在 2,000,000 token、180 活跃分钟预算内完成 paper/review/PDF/export。
- [ ] Writer 已禁止在非全局契约中使用“全局最优”字面短语，待新冻结轮确认不再触发整篇重写。

## [人工]

- [x] 两轮冻结基线均未人工放行错误模型，也未把隐藏 Oracle 放入工作区。
- [ ] 若 post-fix 仍遇到真实图示歧义，只允许记录人工确认点，不得用 Oracle 数值补题。
